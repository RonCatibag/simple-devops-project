projects:
  - name: my-portfolio
    description: "Angular portfolio — build, test & deploy on push"
    repo: ${PORTFOLIO_REPO_URL}
    branch: main
    jenkinsfile: my-portfolio/Jenkinsfile
    agent_label: any
    github_push: true
    folder: angular
    parameters:
      - name: DEPLOY_ENV
        default_value: production
        description: "Target deployment environment"

  # Add more projects below — the seed job picks them up automatically.
  # - name: another-angular-app
  #   description: "Another Angular project"
  #   repo: https://github.com/your-org/another-angular-app.git
  #   branch: main
  #   jenkinsfile: path/to/Jenkinsfile
  #   agent_label: any
  #   github_push: true
  #   folder: angular
